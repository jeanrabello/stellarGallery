# API base URL exposed to the browser.
NEXT_PUBLIC_API_URL=http://localhost:3010/api

# Google Identity Services (real flow).
# Leave empty to keep the dev popup mock at /google-mock.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
